= render "common_content"

- content_for :right_contents do
  = twitter_railway_line_info

- @time_now = Time.now
- @last_update = Time.now
- @next_update = Time.now

= train_location_title_of_each_content
= render( 'ajax/get_new_info' , time_now: @time_now , last_update: @last_update , next_update: @next_update )
%div{ id: "train_informations" }
  - @railway_lines.each do | railway_line |
    = train_information_test( railway_line )
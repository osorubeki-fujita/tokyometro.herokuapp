# データベースのリセット
heroku pg:reset DATABASE_URL --confirm tokyometro
heroku run bundle exec rake RAILS_ENV=production db:migrate
heroku pg:psql

#--------

heroku run rake db:migrate

heroku run bundle exec rake RAILS_ENV=production db:schema:load --app tokyometro
heroku run rake db:schema:load --app tokyometro